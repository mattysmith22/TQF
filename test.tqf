module MBS.TestModule where

global test : string

command "+" (string, string) : string
command "isNotEqualTo" (top, top): bool

--Working on compiling functions only first
function functionName(string arg): string {
    if (arg != "something") {
        return arg + test;
    } else {
        return "something else";
    }
}