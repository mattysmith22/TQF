module MBS.TestModule where

import MBS.Library

global test : string

command systemChat (message: string): nil = "systemChat"
command str (input: top): string = "str"

--Working on compiling functions only first
function functionName(arg: string): string {
    nastyFunction(arg, 1);
    x: (num, string) = (1, "test");
    systemChat(str(missionTime));
    return "string";
}